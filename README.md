# 🚀 Marz Node UI

پنل سبک و ساده برای مدیریت نودهای Marzneshin و تنظیمات Xray از طریق SSH.  
این UI با **FastAPI + Uvicorn** نوشته شده و روی پورت `9000` اجرا می‌شود.

---

## ✨ قابلیت‌ها

- ✅ مدیریت نودها از طریق SSH
- ✅ ویرایش و نمایش JSON مستقیماً داخل UI
- ✅ اجرای خودکار `docker compose up -d / down` بعد از اعمال تغییرات
- ✅ Setup Wizard برای تنظیم اولیه
- ✅ ذخیره تنظیمات در فایل `data/config.json`
- ✅ اجرای سرویس با systemd
- ✅ نصب و حذف تک‌دستوری

---

## ⚡ نصب سریع (تک‌دستوری)

> این دستور همه‌چیز را نصب می‌کند، سرویس systemd می‌سازد و UI را بالا می‌آورد.

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/lornaNET/marznode/main/install.sh)

```
 🗑️ حذف کامل (تک‌دستوری)

> این دستور سرویس، سورس و تنظیمات را کامل پاک می‌کند.


```
bash <(curl -fsSL https://raw.githubusercontent.com/lornaNET/marznode/main/uninstall.sh)
---
